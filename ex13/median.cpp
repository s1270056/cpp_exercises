#include <vectour>
#include <iostream>
#include <algorithm>
#include "median.h"

using namespace std;

int main(void){

    vector<double> num;
    points.push_back(7.0);
    points.push_back(14.0);
    points.push_back(10.0);
    points.push_back(12.0);
    points.push_back(2.0);
    points.push_back(11.0);
    points.push_back(29.0);
    points.push_back(3.0);
    points.push_back(4.0);

    cout << "Median is " << compute_median(num) << endl;
    cout << "k: "

}